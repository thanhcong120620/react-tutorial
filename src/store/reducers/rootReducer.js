const initState = {
  users: [
    { id: "1", name: "Thành Công" },
    { id: "2", name: "Success" },
  ],
};

const rootReducer = (state = initState, action) => {
  return state;
};

export default rootReducer;
